/*  Cyclical figurate numbers
Problem 61
Triangle, square, pentagonal, hexagonal, heptagonal, and octagonal numbers are all figurate (polygonal) numbers and are generated by the following formulae:

Triangle	 	P3,n=n(n+1)/2	 	1, 3, 6, 10, 15, ...
Square	 	P4,n=n2	 	1, 4, 9, 16, 25, ...
Pentagonal	 	P5,n=n(3n−1)/2	 	1, 5, 12, 22, 35, ...
Hexagonal	 	P6,n=n(2n−1)	 	1, 6, 15, 28, 45, ...
Heptagonal	 	P7,n=n(5n−3)/2	 	1, 7, 18, 34, 55, ...
Octagonal	 	P8,n=n(3n−2)	 	1, 8, 21, 40, 65, ...
The ordered set of three 4-digit numbers: 8128, 2882, 8281, has three interesting properties.

The set is cyclic, in that the last two digits of each number is the first two digits of the next number (including the last number with the first).
Each polygonal type: triangle (P3,127=8128), square (P4,91=8281), and pentagonal (P5,44=2882), is represented by a different number in the set.
This is the only set of 4-digit numbers with this property.
Find the sum of the only ordered set of six cyclic 4-digit numbers for which each polygonal type: triangle, square, pentagonal, hexagonal, heptagonal, and octagonal, is represented by a different number in the set.

Pergjigja e sakte:  28684   */

#include <iostream>
#include <vector>
using std::cout;
using std::endl;
using std::vector;

bool gonads[6][10000];

int main()
{
	for(int i=0; i<6; i++)
		memset(gonads[i], false, sizeof(bool)*10000);
	for(int n=0; (n*n+n)/2<10000; n++)
		gonads[0][(n*n+n)/2] = true;
	
	for(int n=0; n*n<10000; n++)
		gonads[1][n*n] = true;
	
	for(int n=0; (3*n*n-n)/2<10000; n++)
		gonads[2][(3*n*n-n)/2] = true;
	
	for(int n=0; 2*n*n-n<10000; n++)
		gonads[3][2*n*n-n] = true;
	
	for(int n=0;(5*n*n-3*n)/2 < 10000;n++)
		gonads[4][(5*n*n-3*n)/2] = true;

	for(int n=0;3*n*n-2*n < 10000;n++)
		gonads[5][3*n*n-2*n] = true;
	
	for(int k=0; k<5; k++)
	{
		bool modit[100];
		bool jaot[100];
		bool yhdistelma[100];
		memset(modit, false, sizeof(bool)*100);
		memset(jaot, false, sizeof(bool)*100);
		memset(yhdistelma, false, sizeof(bool)*100);

		for(int j=0; j<6; j++)
		{
			for(int i=1000; i<10000; i++)
			{
				if (gonads[j][i])
				{
					modit[i%100] = true;
					jaot[i/100] = true;
				}
			}
		}
		for(int i=0; i<100; i++)
			yhdistelma[i] = (modit[i]&jaot[i]);

		for(int j=0; j<6; j++)
			for(int i=1000; i<10000; i++)
				if (gonads[j][i])
					if (!yhdistelma[i%100] || !yhdistelma[i/100])
						gonads[j][i] = false;
	}
	vector<int> nums[6];
	for(int j=0; j<6; j++)
	{
		for(int i=1000; i<10000; i++)
		{
			if (gonads[j][i])
				nums[j].push_back(i);
		}
	}
	for(int a=0; a<nums[0].size(); a++)
	{
		for(int ap=1; ap<6; ap++)
		{
			for(int b=0; b<nums[ap].size(); b++)
			{
				if (nums[0][a]%100 != nums[ap][b]/100)
					continue;
				for(int bp=1; bp<6; bp++)
				{
					if (bp==ap)
						continue;
					for(int c=0; c<nums[bp].size(); c++)
					{
						if (nums[ap][b]%100 != nums[bp][c]/100)
							continue;
						for(int cp=1; cp<6; cp++)
						{
							if (cp==ap || cp==bp)
								continue;
							for(int d=0; d<nums[cp].size(); d++)
							{
								if (nums[bp][c]%100 != nums[cp][d]/100)
									continue;
								for(int dp=1; dp<6; dp++)
								{
									if (dp==ap || dp==bp || dp==cp)
										continue;
									for(int e=0; e<nums[dp].size(); e++)
									{
										if (nums[cp][d]%100 != nums[dp][e]/100)
											continue;
										for(int ep=1; ep<6; ep++)
										{
											if (ep==ap || ep==bp || ep==cp || ep == dp)
												continue;
											for(int f=0; f<nums[ep].size(); f++)
											{

												if (nums[dp][e]%100 == nums[ep][f]/100)
													if (nums[ep][f]%100 == nums[0][a]/100)
														cout << nums[0][a] + nums[ap][b] + nums[bp][c] + nums[cp][d] + nums[dp][e] + nums[ep][f] << endl;
											}
										}
									}
								}
							}
						}
					}
				}
			}
		}
	}
return 0;
}